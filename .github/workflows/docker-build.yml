name: Build and Push CloudPanel Docker Image

on:
  schedule:
    - cron: '0 0 * * *'  # Runs every day at midnight UTC
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual trigger

jobs:
  check-release:
    runs-on: ubuntu-latest
    outputs:
      latest_version: ${{ steps.get_version.outputs.version }}
      needs_build: ${{ steps.compare_versions.outputs.needs_build }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get latest CloudPanel release
        id: get_version
        run: |
          latest_version=$(curl -s https://api.github.com/repos/cloudpanel-io/cloudpanel-ce/releases/latest | jq -r .tag_name)
          echo "Latest release: $latest_version"
          echo "version=$latest_version" >> $GITHUB_ENV
          echo "version=$latest_version" >> $GITHUB_OUTPUT

      - name: Compare with existing version
        id: compare_versions
        run: |
          current_version=$(cat version.txt 2>/dev/null || echo "none")
          if [ "$current_version" != "${{ env.version }}" ]; then
            echo "New version available: ${{ env.version }}"
            echo "needs_build=true" >> $GITHUB_ENV
            echo "needs_build=true" >> $GITHUB_OUTPUT
          else
            echo "Already up-to-date"
            echo "needs_build=false" >> $GITHUB_ENV
            echo "needs_build=false" >> $GITHUB_OUTPUT

  build-and-push:
    needs: check-release
    if: needs.check-release.outputs.needs_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and tag Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/cloudpanel:${{ needs.check-release.outputs.latest_version }} .
          docker tag ${{ secrets.DOCKER_USERNAME }}/cloudpanel:${{ needs.check-release.outputs.latest_version }} ${{ secrets.DOCKER_USERNAME }}/cloudpanel:latest

      - name: Push to Docker Hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/cloudpanel:${{ needs.check-release.outputs.latest_version }}
          docker push ${{ secrets.DOCKER_USERNAME }}/cloudpanel:latest
  
  update-version:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Commit updated version
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          echo "${{ needs.check-release.outputs.latest_version }}" > version.txt
          git add version.txt
          git commit -m "Update version to ${{ needs.check-release.outputs.latest_version }}"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git HEAD:main